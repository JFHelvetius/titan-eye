# Runtime del dashboard para Streamlit Community Cloud.
# Pins amplios a propósito: dejan que pip elija wheels compatibles con el Python
# que use Cloud (3.11–3.13). El paquete `titan_eye` se importa desde src/ vía el
# bootstrap de sys.path en app/streamlit_app.py (no hace falta instalarlo).
streamlit>=1.35
pydantic>=2.7,<3
duckdb>=1.0
pyarrow>=15
sgp4>=2.20
