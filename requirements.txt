# Runtime del dashboard para Streamlit Community Cloud.
# Espejo del requirements.txt de Orbital Sentinel, que despliega correctamente en
# la misma plataforma. El paquete `titan_eye` se importa desde src/ vía el
# bootstrap de sys.path en app/streamlit_app.py (no hace falta instalarlo).
streamlit>=1.35,<2
pandas>=2,<3
pydantic>=2.7,<3
duckdb>=1.0,<2
pyarrow>=15.0
sgp4>=2.20,<3
# Cliente WebSocket para la fuente AISStream (buques AIS en vivo, opcional).
websocket-client>=1.7,<2
